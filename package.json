{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --scope=skytech-web-components5",
    "publish": "npm run build && lerna publish from-package --contents dist --yes --no-verify-access --loglevel verbose"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
