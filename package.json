{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --scope=skytech-web-components4",
    "publish": "npm run build && lerna publish --no-private --contents dist --yes"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
