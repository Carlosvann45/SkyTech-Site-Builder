{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --scope=skytech-web-components6",
    "publish": "npm run build && npx lerna publish from-package --contents dist --yes --no-verify-access --loglevel verbose"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
