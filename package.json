{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/skytech-web-components"
  ],
  "scripts": {
    "update-version": "lerna run update-version --scope=skytech-web-components",
    "build": "npm run update-version && lerna run build --scope=skytech-web-components",
    "publish": "npm run build && lerna publish from-package --contents dist --yes --no-verify-access --loglevel verbose"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
